PORT=3000
BASE_URL=http://localhost:3000
INVENTORY_EMAIL=ktyautopart@gmail.com
MAIL_FROM=AutoParts Hub <no-reply@autopartshub.my>

# SMTP server credentials (required for real emails)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password

# Max allowed payment proof upload size in MB
MAX_PROOF_IMAGE_MB=8

# Set true only for local testing (does not send real email)
TEST_EMAIL_TRANSPORT=false
